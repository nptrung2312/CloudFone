import React from 'react';
import { Link } from 'react-router-dom';

function Profile() {
    return (
        <header>
            <h1>Profile</h1>
        </header>
    );
}

export default Profile;
